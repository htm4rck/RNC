# ─── Aplicacion ──────────────────────────────────────────────────
APP_VERSION=latest
PORT=8050
HOST_PORT=8050
UI_PREFIX=/

# ─── Base de datos ────────────────────────────────────────────────
DB_USERNAME=rcn
DB_PASSWORD=change-me
DB_NAME=remote_career_navigator

# ─── Backend FastAPI ──────────────────────────────────────────────
RCN_APP_NAME="Remote Career Navigator API"
RCN_APP_VERSION="0.1.0"
RCN_DATABASE_URL="postgresql+psycopg://rcn:change-me@postgres:5432/remote_career_navigator"
RCN_CORS_ORIGINS=["http://localhost:4200","http://localhost:8050"]
RCN_STATIC_FILES_DIR="/app/public"
RCN_UI_PREFIX="/"

# ─── Persistencia en el host ─────────────────────────────────────
# Por defecto los datos quedan en ./data/ junto al docker-compose.yml.
# POSTGRES_DATA_PATH=./data/postgres
